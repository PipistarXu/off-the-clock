# off-the-clock
# Off the Clock — Three Rings  A clock with no hands. Time is read as arc length instead of pointer direction: three concentric rings each grow clockwise from the 12 o'clock position and snap back to zero when their unit rolls over.
